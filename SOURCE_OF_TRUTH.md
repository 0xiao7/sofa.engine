# SOURCE OF TRUTH

`sofa.engine` 是 SoFa Engine 的公開前端網站，上線部署到 GitHub Pages 並服務 `sofaengine.org`。

這個 repo 不處理金流、不保存信用卡、不執行 ECPay webhook；付款建立、序號、會員與金流回呼屬於 `sofa-engine-api` / `fay-spectrum-bot` 等後端或機器人服務，勿與舊實驗 repo 或非上線版本混淆。
